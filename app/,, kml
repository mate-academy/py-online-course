Класно, що починаєш — давай по кроках як зробити цей таск.


Створи файл (наприклад online_course.py) і оголоси клас:

class OnlineCourse:
додай метод init(self, name, description, weeks) і збережи аргументи в self.name, self.description, self.weeks.



Реалізуй staticmethod days_to_weeks(days):

Потрібно конвертувати дні в тижні так, щоб часткові тижні рахувались
 як цілий тиждень (наприклад 15 днів → 3 тижні).
Використай цілочисельне ділення з округленням угору.
Підказка: можна використати math.ceil(days / 7) або (days + 6) // 7.



Реалізуй classmethod from_dict(cls, course_dict):

Прочитай name = course_dict["name"],
description = course_dict["description"], days = course_dict["days"].
Порадь перевести days у weeks через cls.days_to_weeks(days).
Поверни новий екземпляр: return cls(name, description, weeks).



Тестування локально:

Створи декілька екземплярів: напр.
course = OnlineCourse("Python", "desc", 2)
course2 = OnlineCourse.from_dict({...})
Перевір print(course2.weeks) для різних значень days: 10 → 2, 14 → 2, 15 → 3.



Кілька підказок по коду (не повні рішення, щоб ти спробував сам):

Для math.ceil потрібно імпортувати math.
Альтернатива без імпорту: weeks = (days + 6) // 7


